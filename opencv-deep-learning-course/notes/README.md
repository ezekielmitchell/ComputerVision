# Notes

Concept notes and write-ups — these count as **artifacts** per the curriculum
("a watched course with no artifact didn't happen", see [`../../CURRICULUM.md`](../../CURRICULUM.md)).

Suggested notes to capture as you go:

- **Classification metrics** (precision / recall / false positives / confusion matrix) —
  from the §08 skim; directly feeds Project 4 (Low-False-Positive Image Classifier).
- **Haar vs. LBP cascades** — when each is appropriate (§06 face detection).
- **Tracking methods** — optical flow vs. MeanShift/CamShift vs. the tracking APIs (§07).
- **Camera-pipeline design** — capture → detect → track → log; FPS and detection-log schema.

One note per concept; link them from `PROGRESS.md` when written.
